{
  "name": "2week_",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^12.1.0",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}
